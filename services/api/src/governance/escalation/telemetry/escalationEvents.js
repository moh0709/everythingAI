const EscalationEvents = Object.freeze({
  ESCALATION_ROUTING_EVALUATED: 'escalation.routing_evaluated',
  ESCALATION_CHAIN_RECORDED: 'escalation.chain_recorded',
  ESCALATION_AUDIT_RECORDED: 'escalation.audit_recorded',
  ESCALATION_STATE_OBSERVED: 'escalation.state_observed'
});

export default EscalationEvents;
